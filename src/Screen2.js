import React, { Component } from 'react';
import { Text, View } from 'react-native';

class Screen2 extends Component {
  render() {
    return (
      <View style={{padding: 8, flex:1}}>
        <Text style={{flex:1}}>This is the films page.</Text>
      </View>
    );
  }
}

export default Screen2;